---
title: '"Зеверс" - техническая документация библиотеки для СибирьОС 1.0'
date: 2025-06-04
category: СибирьОС
layout: post
toc: true
---
## Описание проекта
**Зеверс** - ядро СибирьОС и библиотека для взаимодействия с ней.
## Используемые библиотеки
- LCD_1602_RUS — управление LCD-дисплеем с поддержкой русских символов.
- Wire — I2C коммуникации.
- Arduino — базовая библиотека для Arduino.
- Keypad — работа с матричной клавиатурой.
- IRremote — управление ИК-приемником.
- DS1302 — модуль часов реального времени.
- dht11 — библиотека для датчика температуры и влажности DHT11.
## Распиновка компонентов
<div class="table-wrapper" markdown="block">

|Компонент|Пин|
|:-:|:-:|
|Зуммер|9|
|RGB светодиоды|11 (R), 12 (G), 13 (B)|
|Клавиатура (строки)|21, 20, 19, 18|
|Клавиатура (столбцы)|14, 15, 16, 17|
|ИК-приёмник|41|
|Джойстик X/Y|A2, A6|
|Джойстик (кнопка)|3|
|Датчик влажности почвы|A1|
|Датчик уровня воды|A8|
|Фоторезистор|A0|
|Реле|33|
|DHT11|2|

</div>

## Глобальные переменные
- key, customKey — переменные для хранения текущего нажатия с клавиатуры/ИК пульта.
- lcd — экземпляр дисплея.
- kpd, customKeypad — экземпляры клавиатур.
- rtc — модуль реального времени.
- sensor — датчик DHT11.
- Number, Num1, Num2, result, action — переменные калькулятора.
## Символы на LCD
Кастомные символы создаются через массивы uint8_t. Примеры:
- cub, p, symbol_one, symbol_two, c, c1 — графика и спецсимволы.
## Основные функции
### set()
Инициализация системы. Запуск дисплея, настройка пинов, запуск ИК-приемника, активация реле, датчиков.
### post()
Вывод информации на LCD и тестирование джойстика. Используется для отладки и отображения статуса.
### meteo()
Сбор и отображение климатических данных:
- Освещённость
- Влажность и температура (DHT11)
- Влажность почвы
- Уровень воды (аналоговое значение)
### timell()
Показ текущей даты и времени, дня недели.
### files()
Навигация по "файлам" с помощью джойстика. Реализован выбор и запуск действий в зависимости от направления.
### text()
#### Описание
Функция реализует ввод текста с пульта ИК-управления и клавиатуры. Отображает символы на дисплее lcd.
#### Компоненты
- ИК-приёмник (IRremote)
- Пользовательский Keypad (hexaKeys)
- LCD-дисплей
#### Особенности
- При нажатии определённых кнопок пульта вводятся буквы и цифры.
- Код кнопок пульта сравнивается с предопределёнными значениями (например, 0xFFA25D для q).
- С клавиатуры символы отображаются напрямую.
- Выход по кнопке 0xFB38C7.
### calc()
#### Описание
Функция реализует калькулятор, использующий клавиатуру. Возможны четыре операции: +, -, *, /.
#### Взаимодействие
- DetectButtons() — фиксирует нажатия, собирает числа и операторы.
- CalculateResult() — выполняет вычисления.
- DisplayResult() — отображает результат на дисплее.
#### Управление
- Цифры: от '0' до '9'
- Операции: 'A' (сложение), 'B' (вычитание), 'C' (умножение), 'D' (деление)
- `#` — знак равенства
- `*` — сброс
### cmd()
#### Описание
Функция имитирует ввод команд через клавиатуру customKeypad, имитируя поведение командной строки. Реализует базовое управление реле и системные команды.
#### Примеры команд
- a → включает реле: >>>relon, комментарий //Relay is on
- b → отключает реле: >>>reloff, комментарий //Relay is off
- c, d и другие могут быть реализованы по аналогии
#### Особенности
- Использует блок switch-case для обработки команд
- Использует lcd.init() и lcd.print() для вывода командной строки
## 🧩 Архитектура проекта
### Основные компоненты СибирьОС
- loop() — главный цикл, в котором вызываются все приложения по номеру (через switch(app))
- setup() — инициализация модулей: дисплей, ИК-приёмник, клавиатура и т.п.
- Приложения: реализованы как отдельные функции (text(), calc(), cmd() и др.)
- Глобальные переменные: используются для передачи данных между функциями
### Состояние системы
Хранится в переменных:
- app — номер активного приложения
- flag — состояние внутри приложения (если нужно для переходов)
- input — введённый пользователем текст/команды    
## 🔌 Работа с последовательным портом (Serial Monitor)
### Включение отладки
```cpp
Serial.begin(9600);
```
### Вывод отладочной информации
```cpp
Serial.println("App started");
Serial.print("Received key: ");
Serial.println(key);
```
### Рекомендации
- Использовать Serial.print() для трассировки входа/выхода в функции
- Удалить отладку перед финальной сборкой, чтобы сэкономить память
## 🛠️ Отладка
### Методы
#### Через Serial Monitor
- Убедитесь, что COM-порт выбран правильно
- Используйте флаг DEBUG для включения/выключения отладочных сообщений
#### Логика поэтапной проверки
- Проверяйте модули по одному: ИК, клавиатуру, реле, дисплей
- Убедитесь, что устройства получают питание и правильно подключены
#### LED-индикация
- Используйте светодиоды для подтверждения нажатий или срабатывания функций
